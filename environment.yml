# A Docker environment is provided with DOT, but below you can also find
# a simple conda environment. To use this ...
#
# 1. Install [miniconda](https://docs.conda.io/en/latest/miniconda.html) by selecting the installer that fits your OS version. Once it is installed you may have to restart your terminal (closing your terminal and opening again)
# 2. In this directory, open terminal
# 3. `conda env create -f environment.yml`
# 4. `conda activate promtpflow-serve`
#
name: promptflow-serve
dependencies:
  - pip==23.3.1
  - python=3.11.4
  - pip:
    - -r ./flows/reliefweb_chat/requirements.txt